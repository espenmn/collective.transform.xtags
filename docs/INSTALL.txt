collective.transform.xtags Installation
========================================

* Add ``collective.transform.xtags`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        collective.transform.xtags



  * Re-run buildout, e.g. with:

    $ ./bin/buildout
